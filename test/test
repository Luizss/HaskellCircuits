data Geometry = Triangle Int Int
              | Square Int
              | Circle (Maybe Int)

area :: (Geometry -> Geometry) -> Int
area (Triangle b h)
  | equ b 0 = 0
  | otherwise = mul b h
area (Square l) = mul l l
area (Circle Nothing) = 0
area (Circle (Just r)) = Circle
